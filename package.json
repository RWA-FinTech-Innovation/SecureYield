{
  "name": "secureyield",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:game": "npm run dev --prefix game",
    "sync:game": "npm run build:game && mkdir -p public/game && cp -R game/dist/* public/game/",
    "dev:site": "npm run sync:game && vite",
    "build:game": "npm install --prefix game && npm run build --prefix game",
    "build": "vite build && npm run build:game && rm -rf dist/game dist/3d-demo && cp -R game/dist dist/game && cp -R 3d-demo dist/3d-demo",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.1",
    "eslint": "^10.3.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.6.0",
    "vite": "^8.0.12"
  }
}
